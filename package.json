{
  "name": "monkey-crossing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --bind 127.0.0.1",
    "test": "node --test core.test.mjs",
    "test:browser": "node --test browser.test.mjs",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "playwright": "1.55.1",
    "prettier": "3.6.2"
  }
}
